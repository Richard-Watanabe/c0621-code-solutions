/* exported getFirstChar */

// We need a storage to put the first character in.
// We need to find the first character in the string and store it.
// We need to return the first character of the string that was found.

function getFirstChar(string) {
  var firstCharacter = '';
  firstCharacter = string[0];
  return firstCharacter;
}

getFirstChar('Cody');
