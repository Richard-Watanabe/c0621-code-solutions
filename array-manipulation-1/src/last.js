/* exported last */

// We need a storage for the last data.
// We need to find the last data in the array and store it.
// We need to return what is in the storage.

function last(array) {
  var lastData;
  lastData = array[array.length - 1];
  return lastData;
}
