/* exported head */

// We need a storage for the first data in the array.
// Find the first data in the array and store it.

function head(array) {
  var firstData;
  firstData = array[0];
  return firstData;
}
